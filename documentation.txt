Household Chore is a web app to manage your home tasks and ideas.
It is build in python and use flask to build the view modules together with SQLAlchemy which is the querrier module to
controle the database, in this case the DB is SQLServer.
