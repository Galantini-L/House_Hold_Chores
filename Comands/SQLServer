LOGIN is used for attentication in SQL Instans, while a USUR is for authorization into a SQL Database 


create user:

create login:



LIST USERS:

opcion1:	SELECT * FROM master.sys.database_principals;
	
	
opcion2:	select name as username,
       		create_date,
      		modify_date,
       		type_desc as type,
       		authentication_type_desc as authentication_type
		from sys.database_principals
		where type not in ('A', 'G', 'R', 'X')
      		and sid is not null
      		and name != 'guest'
		order by username;



LIST LOGIN:

-	SELECT * FROM master.sys.sql_logins;


LIST DATABASES:

-	SELECT name FROM sys.databases

LIST DATA TABLES FROM DATABASE:

-	SELECT TABLE_NAME 
	FROM [<DATABASE_NAME>].INFORMATION_SCHEMA.TABLES 
	WHERE TABLE_TYPE = 'BASE TABLE'


LIST SERVERNAME AND OTHERS:

	SELECT @@SERVERNAME,
    	SERVERPROPERTY('ComputerNamePhysicalNetBIOS'),
    	SERVERPROPERTY('MachineName'),
    	SERVERPROPERTY('ServerName')




HouseHoldChores
House@Hold
